syntax = "proto3";
import "Color.proto";
message BTMessage {
  enum Type { COLOR = 0; BAR = 1; BAZ = 2; }

  // Identifies which field is filled in.
  Type type = 1;

  // One of the following will be filled in.
  Color color = 2;
  //optional Bar bar = 3;
  //optional Baz baz = 4;
}
